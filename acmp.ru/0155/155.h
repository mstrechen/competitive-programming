#include <iostream>
#include <vector>

using namespace std;

double prec(vector<double> v)
{
	if(v.size()==2)
	{
		
	}
}

double c[7];
int main(){
	ios::sync_with_stdio(false);
	int n;
	cin >> n;
	for(int i = 0; i<n; i++)
	{
		cin << c[i];
	}
	
	return 0;
}