#include <iostream>

using namespace std;
unsigned __int64 m=1,x,a,b,x0;
int main(){
	ios::sync_with_stdio(false);
	int k;
	cin >> k >> x0 >> a >> b >> x;
	if(b==0)
	for(int i = 0; i<k; i++)
	{
		m<<=1;
		m++;
	}
	
	return 0;
}