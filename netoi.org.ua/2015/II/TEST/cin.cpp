#include <iostream>

using namespace std;

int main(){
	ios::sync_with_stdio(true);
	int n,k,l,tmp;
	cin >> n >>k >> l;
	for(int i=0;i<n;i++)
		for(int j=0; j<n;j++)
		{
			cin >> tmp;
		}
	cin >> k;
	for(int i=0;i<k;i++)
		cin >> tmp;
	cout << 1 << ' ' << 1 << ' ' << 1;
	return 0;
	
}