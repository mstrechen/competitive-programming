#include<iostream>
#include<vector>
#include<bitset>


using namespace std;

void dfs(int i, vector<vector<char> > & M)
{

}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);


    int n;
    cin >> n;

    vector<vector<char> > A(n,vector<char>(n,0));

    for(auto & i : A)
        for(auto & j : i)
            cin >> j;

    f

    return 0;
}
