#include<iostream>
#include<vector>
#include<set>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while(t--)
    {
        int n;
        cin >> n;
        multiset<int> allDepths;

        vector<int> depth(n,0);
        vector<int> maxDepth(n,0);
        vector<int> global_parents(n,0);

        for(int i = 1; i<n; i++)
        {

        }




    }

    return 0;
}
