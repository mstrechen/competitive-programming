#include <iostream>

using namespace std;

int a[1000], b[1000];
int main(){
	ios::sync_with_stdio(false);
    int n;
    cin >> n >> k;
    for(int i = 0; i<n; i++)
        cin >> a[i];
    for(int i = 0; i<n; i++)
        cin >> b[i];
    for(int i = 1; i<1001; i++)
    {
        bool isOk = 1;
        int needed = 0;
        for(int j = 0; j<n; j++)

    }
	return 0;
}

